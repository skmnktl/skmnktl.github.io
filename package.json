{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "push-dir --dir=build --branch=gh-pages --allow-unclean --cleanup"
  },
  "devDependencies": {
    "html2pdf.js": "^0.10.1",
    "nuxt": "3.0.0",
    "push-dir": "^0.4.1"
  },
  "dependencies": {
    "@fortawesome/vue-fontawesome": "^3.0.2"
  }
}
